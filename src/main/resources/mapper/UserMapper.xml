<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yidiansishiyi.aimodule.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.yidiansishiyi.aimodule.model.entity.User">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userAccount" column="userAccount" jdbcType="VARCHAR"/>
        <result property="userPassword" column="userPassword" jdbcType="VARCHAR"/>
        <result property="userName" column="userName" jdbcType="VARCHAR"/>
        <result property="userAvatar" column="userAvatar" jdbcType="VARCHAR"/>
        <result property="userRole" column="userRole" jdbcType="VARCHAR"/>
        <result property="userMailbox" column="userMailbox" jdbcType="VARCHAR"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="isDelete" jdbcType="TINYINT"/>
    </resultMap>



    <sql id="Base_Column_List">
        id
        ,userAccount,userPassword,
        userName,userAvatar,userRole,userMailbox,
        createTime,updateTime,isDelete
    </sql>

    <select id="userListzer" resultType="java.util.LinkedHashMap">
        select
            id as id,
            userAccount as `3_0用户名`,
            userPassword as `2_0密码`,
            userName as `姓名`
        from user where id = '1672600130722787330';
    </select>

<!--    where id = '1672600130722787330'-->

    <select id="getSqlTable" resultType="string">
        SELECT table_name FROM information_schema.tables WHERE table_schema = #{databaseName};
    </select>

    <select id="getSqlStructure" resultType="map">
        SHOW CREATE TABLE ${tableName}
    </select>

</mapper>
